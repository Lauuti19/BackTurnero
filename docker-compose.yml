version: '3.8'
services:
  backend-turnero:
    build: .
    ports:
      - "3001:3001"
    env_file:
      - .env
    restart: always
    networks:
      - turnero-network

networks:
  turnero-network:
    external: true
