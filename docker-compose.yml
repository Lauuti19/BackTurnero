services:
  backend:
    build: .
    ports:
      - "3001:3001"
    env_file:
      - .env
    networks:
      - turnero-network
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped
    environment:
      - DB_HOST=db
      - DB_PORT=3306